{
  "name": "kresty-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run tokens:check && tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint:tokens": "node scripts/lint-tokens.mjs",
    "lint:tokens:update": "node scripts/lint-tokens.mjs --update",
    "tokens": "node scripts/build-tokens.mjs",
    "tokens:check": "node scripts/build-tokens.mjs --check"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "@webgpu/types": "^0.1.51",
    "playwright-core": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
