# Changelog

| Iteration | Date | Summary |
|-----------|------|---------|
| 18 | 2026-08-01 | **Map rounds 9 → 9.5** — the flat site plan, geographic north, Figma captions, and four water treatments. New GLB `map-w-river.glb` adds the Neva, both streets and the neighbouring blocks as zero-thickness surfaces. **The "must not distort on cursor movement" requirement cost nothing**: the plan-oblique shear `x' = x + sx·y` is the identity at `y = 0`, so flat geometry on that plane is pointwise invariant under any lean — and the GLB puts all three flat prims exactly there. The same geometry becomes real ground in focus mode, because focus rotates the *camera*. Proven numerically, not by eye: plan regions **byte-identical** across cursor-centre → corner → opposite corner, while a control region over the west cross moves max 140–158. **The node transform must be BAKED, not discarded** — this GLB is a Blender Z-up export whose orientation lives entirely in the node quaternion `[0.5,−0.5,0.5,0.5]`; the old loader dropped it and only got away with it because `map.glb` happened to ship Y-up. North = **189°** (half turn + 9° street grid), checked two ways against the Yandex plan, and baked into the same matrix so every downstream bbox/centroid/`axisAngle` lands in world frame. Framing measured on the **buildings alone** — the plan spans ~3× their footprint and would otherwise shrink the volumes to a third. Building ids verified unchanged (19 components, identical triangle counts), so `buildingsInfo.ts` keys survive. **Pier flicker** root-caused: `b18` is a 0.09-tall slab whose base is exactly on the plane, so the bottom face `DoubleSide` renders is coplanar with the river and per-pixel float error decides the winner — fixed with `polygonOffset`, which biases only the depth *value*, never the vertex, keeping the invariance intact. Palette sampled from the designer's snapshot (water `#d4eaf5`, blocks `#ccd7dd`, streets `#f5f5f5`, ground plate `#dde6e9`); the plate needed `toneMapped: false` too, since `NeutralToneMapping` darkened it while `MAP_BG` (the clear colour) went untouched. **Captions** (`mapLabels.ts`) are derived from geometry, never authored — each feature recovered as a connected component, anchored to the nearest *surface* point (the perpendicular foot; the nearest vertex lands on a corner and can sit at the far end of a street). Rebuilt to Figma `477:440`: ALS Chromius **120**/22px (the `wght` trap again — 400 clamps to Black), streets `#a8bac1`, river `#5992ab`, river horizontal. **A real bug behind the rotation**: `footprintAxis` returns an eigenvector `(cos θ, sin θ)` in (x,z) but `axisVector` built `(cos θ, 0, −sin θ)`, mirroring every caption's tilt — it hid because the cross blocks genuinely lean the other way. `MapCamera.focusOn` shares the convention mismatch and is deliberately untouched (four diagonals 90° apart ⇒ still a valid pose). **Water: four switchable modes** behind `?water=`, differing in look *and* technique — `lines` (procedural `fwidth` isolines, 0 VRAM, **shipped default**), `chop` (baked tiling FBM, 341 KB), `glints` (`THREE.Points`), `gloss` (analytic wave normals + specular). The original sine drift was invisible for a *frequency* reason: at ~2.7 px per world unit its 26/17/41-unit waves rendered at 71/46/111 px, so no amount of amplitude could have saved it. Two recurring lessons logged: **summed sines interfere into plaid** (killed twice), and **check the range of a quantity before ramping it** — `gloss` showed no specular because a flat normal already scores `dot(N,H)=0.85` against this key light, needing a ~32° tilt to fire. Perf measured on a real M1: default path is 0 VRAM and 0 bake; `chop` costs 341 KB, 17.9 ms once, and **0.000 ms/frame** (both vsync-locked; `mapPixelRatio` caps the backing store at 2×). |
| 17 | 2026-07-30 | **Main round 9** — all three transitions killed, one plain cross-fade. Verdict on 8.2 was *"all the slider options are bad and buggy"*, so `transitions.ts`, the `.tr-switch` picker, `?tr=`, the T key, `PhotoSlider.preview()`/`setTransition()`/`transitionIndex`, the `.entering` class and every `tr-naplyv`/`tr-stvor`/`tr-sdvig` rule (plus the `--stvor-arm`/`--stvor-w` `@property` registrations) are **deleted**. **The defect was sequential staging, not the effects** — 8.2 ran `openMs → effectMs → closeMs` (3050 ms on «Створ») with the reveal held behind `animation-delay: var(--tr-open)`, leaving a beat where the text had gone, the scrim had opened and the photos had not started; that gap *is* the "lag in the middle", and a nicer effect would not have touched it. **Any future transition here must overlap its beats.** Round 9 does: heading-out + scrim-open at t=0, cross-fade from 150 ms, scrim-close + new heading the instant the fade lands at 950 ms, next throw at 6 s (0.8 s fade / 6 s hold, the designer's pick). **Mechanism: one animated element on one compositor-only property** — the incoming photo sits UNDERNEATH at opacity 1 from frame one and only the outgoing fades 1 → 0, on `cubic-bezier(0.45, 0, 0.55, 1)`. That asymmetry is load-bearing twice over: the lower layer being opaque and covering the frame makes `#56b7e6` unreachable *by construction*, and it is why there is no midpoint dip — fading BOTH layers would sum to < 1 and flash the backdrop. The scrim choreography is the round-8.1 one unchanged in shape (it already *was* "the overlay follows the heading"); only its depth changed, opening fully to **0** instead of 0.06. `scrim-open-first` stops being an animation — on the first throw there is nothing to open *from*, so it holds at 0. **All six durations live in `PhotoSlider.ts` and are pushed to `--tr-*` customs once, on `#screen-main` rather than `.photo-slider`** — the headline lives in the stage, outside the slider subtree, and needs `--tr-out` to inherit; so there is no JS↔CSS mirror left. `SLIDE_DIP_S` 1.6 s → 1.1 s so the half-sine's peak lands on the new fade midpoint (550 ms). **Verified:** 0 stalled samples across a full throw once the `.word` spans are sampled too (the first probe reported two stalls at 1008/1747 ms — both its own blind spot, the words are the only thing moving there); outgoing opacity monotonic; lower layer min opacity 1.000, covering and decoded; scrim max 0.400 / min 0.000; **median 16.7 ms, worst 17.7 ms, 0/841 frames over 22 ms** (8.2's worst was 18.8 ms). |
| 16 | 2026-07-30 | **Main round 8.2** — voronoi killed; three transitions behind a picker. The nine-lobe `mask-composite: intersect` dissolve is **deleted** (all `@property --burnN`, `photo-burn`, `.dissolving`/`.settling`, `supportsOrganicDissolve()`, the `dissolve-simple` fallback and `?ds=`) — rejected on looks, and it was also the only thing on this screen that could drop frames, since CSS masks rasterize on the **paint thread** at (layers × element pixels) every frame and never touch the compositor. **Three replacements, chosen along the axis of PACE**, in a new `transitions.ts`: **«Наплыв»** (slow, 2.2s / 9.5s hold — the old photo defocuses out of existence), **«Створ»** (medium, 1.3s / 8s — a cross-shaped slit opens out of the light's convergence point and the new photo floods through; default, and the on-brand one since the cross is centred exactly where round 7 put the light, so it reads as the light carving the next photo in rather than a slideshow wipe), **«Сдвиг»** (fast, 0.8s / 7s — the new photo pushes in along the light's diagonal). No masks: «Створ» is one `clip-path` polygon per frame driven by two registered `<percentage>` customs; «Сдвиг» is `opacity` + `transform` on the incoming. All three measured **median 16.7ms, worst 18.8ms, 0 frames >22ms** with the ray field live. **Two structural rules** the specs obey: `incomingOnTop` sets the z-order and whichever layer is underneath must stay opaque and cover the frame (so an effect may scale the lower layer up but **never translate it** — «Сдвиг» translates the *incoming* photo, which is on top, for exactly this reason); and the reveal is phased by `animation-delay: var(--tr-open)` with `fill-mode: both`, **not** a JS timer — without `both` an `incomingOnTop` effect hard-cuts for the whole opening beat because `.visible` puts it at opacity 1 before its 0% keyframe applies. **Durations are CSS customs, not keyframe percentages** (percentages can't be var-driven, `animation-duration`/`-delay` can), so the scrim split into `scrim-open`/`scrim-open-first`/`scrim-close` and one rule set serves any pace; `photo-drift` reads `--tr-hold` to stay in step. The round-8.1 scrim choreography is unchanged and mechanism-independent — only the reveal in the middle was replaced. **Controls:** `.tr-switch` ships visible (T hides it; V still toggles `.fx-switch`, which moved to `bottom:64px` so the two stack), and clicking a button calls `preview()` to throw a slide immediately rather than making the designer sit out the 7s idle wait. Verified by computed-style probe rather than eyeballed pixels — the first visual pass wrongly suggested «Сдвиг» was hard-cutting when it was simply finishing inside the screenshot latency. |
| 15 | 2026-07-30 | **Main round 8.1** — fixed the "laggy" handoff, the end-of-dissolve stall, the hover gate and the type rags. **The dark beat was backwards:** 8.0 dipped to 0.62 — *darker than the resting 0.40* — during the window when the old text had already gone and the dissolve had not started, so the screen was at its darkest with nothing moving. Inverted: the scrim now **opens as the text melts up** (measured: scrim 0.10 at 416ms, text gone at 433ms — in sync), the dissolve runs at 700–1900ms on a fully bright photo, and the scrim closes with the new text; **nothing on the track is ever darker than the resting 0.40** and there is no hold or gap. `HEADLINE_OUT_AT_MS` deleted — the text now leaves *at* the throw, so a cycle is one event, not two. **The end-of-dissolve stall was self-inflicted:** CSS masks rasterize on the paint thread, so cost is (layers × element pixels) every frame, and 8.0 stacked its three most expensive things at the very end — 9 gradient layers at max radius, `blur(26px)` at peak, and a `scale(1.03)` forcing re-raster — then tore the element down while it was still fully visible. Fixed by **9 lobes → 6** (the circle fix was the *spread* and the 48% feather, not the count), **peak blur 26→16px**, the swell removed, `will-change` removed (`filter` already promotes; declaring it on a per-frame property costs a persistent tile plus a teardown), and — the actual fix — an **opacity tail** fading the photo out over the burn's last 30%, so the most expensive frames are the ones nobody sees and the element is already invisible when its mask is dropped. **Slider no longer activates while a nav link is hovered** (`setHoverBlocked`, single `tryActivate()` gate on `idleElapsed && !hoverBlocked`) — it previously took the screen on top of the hover scene, which also collided with `.hover-scene` sitting z:2 above it. **Type, from actually reading the Figma renders rather than counting lines:** line height is a flat **64px, not 1.15** (Figma reports `leading-[1.15]` but its boxes are 192/3 and 256/4 — 56×1.15=64.4 drifts 0.4px per line), and **slide 2's rag didn't match** because «для» was being orphaned. `bindOrphans` became `shared/ruTypography.ts` → `bindShortWords()`, covering 1-letter words *and* the 2–3 letter prepositions per Мильчин / «Ководство» §62 (particles excluded — those must not *start* a line, the opposite direction), now applied to the **news block** too («по демонтажу», «для начала», and «1 ноября» on the date row). **All 8 headline rags and both box heights now match Figma exactly** (192/192, 256/256); news re-checked and was already exact (270 wide, 27px lines, rows 27/81/27, block 151). Verified: build + smoke test clean, 9/9 slide pairings, hover gate passes 4/4 cases, both dissolve paths median 16.7ms / worst 18.6ms / 0 frames >20ms. |
| 14 | 2026-07-30 | **Main round 8** — final copy/photos, 1:1 layout, organic dissolve (Figma section `366:92`). **Root-caused a site-wide font bug:** `ALS_Chromius_VF_0.8.ttf` has a **non-standard `wght` axis (min 50 / default 120 / max 232**, Regular=120, Medium=150), so every `'wght' 500` rule since round 0 clamped to 232 = **Black** — all Chromius text has been three weights too heavy for eight rounds. Hidden by the loose 692 px headline box; exposed by tightening it to Figma's 580 px. Fixed via `@font-face { font-weight: 50 232 }` + plain `font-weight: 150/120` and **no `font-variation-settings`** (that property overrides `font-weight` and is how the trap was set). With the axis right, all 8 headlines wrap to Figma's line counts exactly. **Layout 1:1:** logo `top:40→32` / `301.44×48`; news `270` wide, leading `1.35`, Chromius Regular, **plus the date row the mockup always had** (`NEWS_HEADLINES`→`NEWS_ITEMS{date,text}`); headline `left:32 w:580` **centred on y=400** rather than top-pinned (Figma's 192/256 px boxes share a centre, so the exit transform carries the `-50%`); nav dim `0.5→0.4`; the «КРЕСТЫ · 2026» text placeholder replaced by the real **ARTLEBEDEV vector**. Nav link positions untouched — already correct to 0.4 px against the *slide* frames. **Photos:** 8 slides hard-bound to their headlines, `concept-plan` reserved for the «Концепция» hover, others repointed; PNG→WebP by **centre-cropping to 3:2 then capping at 2400** — the crop is the win, since square 2560² renders lose ~44% of their height to `cover` at paint time, i.e. bytes shipped to be discarded; **9 files / 3.1 MB vs 10 MB for the four PNGs they replace**; preload cut to 2 up front + 1 ahead. **Handoff rebuilt:** the ×2.4 godray surge is **deleted** (it fired in the empty gap between headlines — that *was* the "flash at the end") and replaced by a **dip** that joins the darkness beat. The incoming photo now sits **underneath at opacity 1 from frame one**, making the flat blue structurally unreachable (measured 0% blue pixels across a handoff); the outgoing is *erased* by **nine soft `radial-gradient` holes unioned via `mask-composite: intersect`**, radii animated as typed `@property <length>` customs in `vmax` — plain CSS can't interpolate gradient stops, a registered property can. **First attempt read as visible circles** (six big centre-clustered lobes, 22% feather) → nine smaller spread lobes + a **48% feather** + front-loaded blur. **One dark layer serves as both the transient dip and the Figma 0.4 scrim**, arriving late by client request so a fresh photo shows briefly unscrimmed, with the headline timed to the scrim closing: measured mean luminance **98 → 79 → 151 → 109**, i.e. it sinks before it rises. Plus a 3.5% scale-down across the dwell and U+00A0 orphan binding (which is what reproduces the designer's rag). Two traps found: `.settling` and `.dissolving` **collide on the `animation` shorthand** (so `photo-burn` never ran and the old photo hard-cut — hence scale and mask on separate elements), and re-entry after an interrupt left the old photo **opaque on the top layer** with its mask stripped. Verified: tsc + build + smoke test clean; both dissolve paths **median 16.7 ms, worst 18.7 ms, zero frames >20 ms** with the ray field live. |
| 13 | 2026-07-28 | **Map round 8** — isometric focus view. Selecting a building zooms it into the space right of the drawer, dims everything else (`MAP_LOOK_DIMMED`, a fourth state in the existing material-swap mechanism), and swings the camera to a **classic isometric angle** (35.26° elevation). **The round-4 "camera never rotates" rule is suspended in focus mode only** — agreed with the designer; it protects the overview's pixel-exact roof plan, which is verified still intact. Three things animate together: shear → 0, orientation → isometric, frustum → framed on the building (shear and rotation compound into a skewed mess otherwise), so the cursor lean is inert while focused and resumes on exit. **The isometric angle incidentally fixes the flat-roof complaint for the focused building** — roof plus two walls finally have different normals, so the raking key gives three tones. New `mapCamera.ts` **slerps orientation rather than animating azimuth/elevation**, because an orbit parameterisation is degenerate at 90° elevation — exactly where the overview sits — and uses **critically-damped springs** rather than `easing.ts`'s fixed-duration `tween` so the camera can retarget mid-flight when a second building is clicked. Per-building view angle from the footprint's principal axis (PCA over XZ, since several blocks are rotated and their AABB says nothing about facing), picking whichever of the four diagonals is nearest the current azimuth so clicks swing the short way. Two bugs found and fixed en route: **`Object3D.lookAt()` points +Z at the target while camera `lookAt()` points −Z**, so building poses from a plain Object3D came out reversed and put the camera under the model (caught because the overview rendered mirrored); and tightening the sliver filter **renumbered part ids** from the first dropped part onward, silently shifting building labels. 19 buildings, `b00`–`b18`, all named. 60 fps through transitions. |
| 12 | 2026-07-28 | **Map round 7** — «Грани» only, raking light, clickable buildings. Collapsed to the designer's chosen variant: «Кристалл», «Огранка», the segmented control, `?look=` and the whole bloom chain deleted. **Flat roofs root-caused to TWO things, the light angle being the smaller:** the key sat above 50° (moved to ~24° raking, fill cut to 0.4), but the real limiter was **clipping** — the renderer ran `NoToneMapping` while the environment probe alone contributes ≈3.85 linear, so every lit face was already past the clip point and raising the key added brightness but no gradation (a test at intensity 7 washed out further). Fixed with **`NeutralToneMapping`**. **Buildings separated** by connected-component analysis of the welded geometry (`buildingSplit.ts`) — the GLB has no per-building nodes — 27 raw components → **19 buildings**, absorbing slivers **by footprint rather than triangle count** (the church's columns are 44 tris but 0.02×0.12, so a triangle threshold wrongly kept them as buildings). IDs order by triangle count, i.e. modelling detail not size, so `b00` is the domed church and the cross blocks are `b01`/`b02` — a first pass assuming size order mislabelled a cross as «Церковь». **Hover + click + left resident drawer**: picking runs per frame (the shear moves geometry under a stationary cursor), `LineSegments2.raycast` stubbed out so fat lines don't intercept hits, three body + two edge materials swapped by reference, and the drawer freezes the lean while the pointer is over it (`DEADZONE` is 0, so it would otherwise keep tilting while you read). Splitting made picking *cheaper* — bounding-volume rejection per mesh; measured 60 fps with per-frame raycasting. `ConceptScreen.ts` kept under the 350-line rule by extracting `buildingPicker.ts`; new `buildingSplit.ts`, `buildingsInfo.ts`, `BuildingDrawer.ts`. |
| 11 | 2026-07-28 | **Map round 6** — cut-crystal look, three comparable variants behind a visible segmented control (`?look=<id>`). Root-caused the flat render: (1) the GLB's normals are **73.9% smoothed across hard edges** in primitive 1, so shading was blurred exactly where architecture should read sharpest → `toCreasedNormals(35°)` at load, the single biggest win; (2) a smooth gradient environment **cannot** produce sharp speculars → replaced with an **HDR `DataTexture`** studio probe (a `CanvasTexture` clamps at 1.0, and the over-1.0 overdrive is what makes speculars blow out white), carrying softboxes plus **cross-shaped emitters** so the glass throws the project's own motif as glints; (3) `transmission` makes transmissive meshes invisible to each other, which is structurally why buildings couldn't be seen through one another. Source placement follows what an ortho top-down camera can actually see reflected — flat roofs mirror the zenith exactly (so sources there brighten uniformly rather than glint), sheared walls reflect just below the horizon (so that band carries the strongest sources). Variants: **«Кристалл»** (physical glass only), **«Огранка»** (+ `UnrealBloomPass`; threshold **must** exceed 1.0 — 0.85 bloomed the near-white field itself into mush, and EffectComposer's HalfFloat buffer preserves the >1 speculars), **«Грани»** (alpha fill + ~7,263 `LineSegments2` fat edge lines, genuinely see-through, and the *cheapest* — no transmission pass). Field moved to a near-white studio `#f2f6fa`; overlay UI recoloured deep blue since it was white and would have vanished. `ConceptScreen.ts` split 505 → 352 lines across new `mapLooks.ts` / `mapStudio.ts` / `edgeLines.ts`. Measured: all three at **median 16.7 ms (60 fps)**, p95 ~17.6 ms; low tier never builds the composer. |
| 10 | 2026-07-28 | **Map round 5** — new GLB, fitted framing, sky-blue glass. Geometry swapped to `map.glb` (16,903 tris; `scene.glb` deleted). **Framing root-caused:** `resize()` built a 248-unit-tall frustum for a 263-unit-deep model, so the plan was clipped before shear even applied, and it derived height only (narrow windows clipped horizontally too). Replaced with a real fit against the model's *measured* bounds plus worst-case shear reach on BOTH axes (`FIT_MARGIN`, `?fit=<k>`); `ZOOM` and the `0.62` magic number deleted. **Stretch halved:** `MAX_SHEAR 1.6 → 0.8` (at 1.6 the cross blocks smear together and stop reading as crosses — confirmed via `?ob=1.6`). **Stretch response** is now a saturating exponential (`RESPONSE_GAIN 3.5`, `SMOOTH_TAU 0.25 → 0.12`): cursor magnitude 0.2 gives 37.8% of full lean vs 13% before, still hard-clamped. **Materials:** sky-blue refractive glass (`MeshPhysicalMaterial`, `transmission 0.7`) replacing the unlit vertex-colour height gradient (`splitByHorizontal`/`paintHeightGradient` deleted, ~65 lines), a world-fixed key/fill/rim rig, and a code-built equirect sky-gradient environment probe replacing `RoomEnvironment` — under a top-down camera roofs mirror the environment *straight up*, so a white studio probe turned the whole plan grey, and the gradient's zenith colour is now literally the roof colour. Two r170 traps documented: `transmission` replaces the diffuse lobe (so `color` does nothing at 1.0), and `renderTransmissionPass` clears to **white-0.5 whenever `clearAlpha < 1`** — hence an opaque `#060910` clear + `scene.background`, matched by the CSS backdrop. Added an unlit gradient ground plate because only *opaque* objects enter the transmission target, so glass over a bare backdrop refracts nothing. Verified headless at 1440×800 / 1100×800 / 900×900, rest + all four corners; build + interact smoke test clean. |
| 9 | 2026-07-27 | Main-screen **round 7** (Figma node 257:116). **Centered:** the light convergence + nav cross dropped the +40/+20 offset for the exact stage center (`CENTER_X/Y = STAGE_W/2, STAGE_H/2`), so the hero sits at the exact pixel center on any viewport; nav links re-anchored on the ±45° diagonals. **Slow rotation:** the whole light cross turns continuously (~0.6°/s, `ROT_SPEED`) via a new time-driven `signRot` state field — WebGL2 `u_signRot` uniform / WebGPU free `p9.w` slot, no buffer growth — rotating the slit-mask sample vectors `p`+`par` (never `q`/lean) so cursor wind/parallax stay screen-true on top; it also replaced the old ±8° sway on the procedural `beamAngles`. **Hover = engraved echo:** the label no longer goes black — it stays white with a breathing shine and an outline-only `::after` copy (`attr(data-label)`, 1 px white text-stroke) slides 3 px along the glyph axis, echoing the logo's doubled contour; the gallery-dark scene still fires. **Slider star KILLED:** `StarSlider`→`PhotoSlider`, one full-bleed nadir photo per slide (`main-1a..4a`), star layer/mask/`star.svg` deleted; idle delay 4.2→7 s; headline moved to the left column (56 px / lh 1.15, Figma) and reveals **word by word** (pure-CSS `.word` spans, 80 ms stagger, rise-out-of-blur on the airy curve; exit melts up as one). Verified: WebGPU + WebGL2 (centering at 1440×800 and 1100×800, ~24° rotation over 38 s coexisting with lean, per-word opacity cascade, hover echo), tsc + build clean, interact smoke test (fixed to press V for the hidden switcher). |
| 8 | 2026-07-23 | Pitch defaults: **«Слайдер» is the default variant** (`DEFAULT_INDEX` derived from the `slider` id) and the segmented control ships hidden (`.fx-switch.hidden`). Dev shortcut: the physical **V key** toggles the switcher (`e.code === 'KeyV'` — layout-independent, works on the Russian layout; Cmd/Ctrl/Alt chords like paste are ignored; the listener attaches in `MainScreen.start()` and detaches in `stop()`, so it is inert on «Концепция»). `?fx=` dev override unchanged. Verified headless (WebGPU): hidden by default with «Слайдер» active, V shows/hides, buttons functional while visible, Cmd+V ignored, V inert on the map screen, `?fx=prism` still overrides, unknown `?fx=` id falls back to «Слайдер». |
| 7 | 2026-07-23 | Main-screen light v6 — **«Слайдер»**: a 4th switcher tab (`SWITCHER_COUNT = 4`) pairing the «Сияние» light with a new idle photo slider — each slide a zenith shot full-bleed behind + a nadir shot inside a 4-pointed star mask (`src/assets/star.svg`, points aligned to the beam bisectors) plus a headline, new `StarSlider` component and slide data in `layout.ts`. New **«Проектор» choreography**: the light throws each slide (flash synced with the star scaling out of the light centre, 0.5 s expo-out / 0.25 s collapse / 6 s hold) then retreats to a dim ember between slides — CPU-side param modulation in `MainScreen.update` only, zero shader changes, both twins untouched. Real client photos (`public/resources/main-*.png`) replaced ALL gitignored placeholders and are now committed; the crossfade showreel (tabs 1–3) and the hover "gallery dark" per-link images were re-pointed at them. **Round 6.1 (same day):** the ember was replaced by true **occlusion** — the ray canvas gets a two-layer feathered star mask (`mask-composite: exclude`) animated in lockstep with the star's transform, so the FULL-intensity light physically breaks around the star's contour; all curves reworked airy (throw 1.2 s long-tail ease-out from scale 0.35, recede 0.6 s in-out, bg 1.4 s, headline fade+16 px drift, hold 7 s), flash softened to a breath (×1.4/×1.0/×0.7 @ τ 0.3 s) and gated to the slider tab. **Round 6.2 (same day):** occlusion KILLED on user verdict ("weird and cheap") — the light now shines untouched; slide change reworked to **dissolve-in-air** (two double-buffered star wraps: the old star fades + drifts to scale 1.06 + blurs to 14 px while the next blooms up behind it 0.6 s in — never a scale-down); bloom made adult (1.6 s `cubic-bezier(0.3,0,0.12,1)` from scale 0.55, zero overshoot); headline rises in on the same curve and dissolves upward like vapor on exit. **Round 6.3:** the star moved ABOVE the light (own `.star-layer` after the canvas at the same z — the light shines over the photos and disappears behind the star, plain stacking); dissolve blur moved to the mask's parent so the whole star shape melts (contour included); bg change made glitch-free (incoming photo fades in on top instead of a symmetric crossfade; same-src changes skipped — slides 1/3 share the sky). Verified: WebGPU + WebGL2, regression clean. |
| 6 | 2026-07-21 | Main-screen light v5 — **«Сияние»**: the emblem dissolved into zoom-blur light trails (round-5 verdict on «Прорезь»: "too logo with some glow"). New `dissolve` param (free `p9.z` slot, no buffer growth): crisp core ×(1−dissolve), `rayGate` opens (blown bright centre), longer god-ray decay, wider bloom, radial melt at screen edges; in `NUMERIC_KEYS` so the switcher morphs crisp↔dissolved. **"Ladders" root-caused + fixed** (benefits «Прорезь» too): the god-ray march's N uniform steps and the 12 fixed spiral bloom taps deposited offset ghost copies of the mask edges — per-pixel STATIC jitter (dithered march start, rotated spiral, `hash21(fragPx)`) converts the banding to grain-covered noise. **Explosive appearance burst** replaces the "funny" scale-in: CPU-side envelope on slit-variant activation — signSize smashes 0.25×→1× in ~0.25 s under a ×4/×3/×2.5 godrays/bloom/core flash decaying in ~0.5 s. **Noise follow-up:** the jitter's stipple (high variance of sparse taps on a hard-edged mask) fixed by a three-channel mask — R crisp emblem (core), G round blur (bloom halo), B **radial smear** (feeds the god-ray march via `signMaskRay`) — an isotropic blur killed the noise but melted the razor-sharp trail edges; smearing only along the march direction removes the variance the march sees while keeping the tangential edges sharp. Rule: the pre-filter must be aligned with the integration direction. Switcher now 3 items (Сияние default · Прорезь · Призма). Verified: tsc, build, screenshot suites on WebGPU + WebGL2 (wish-image match at rest, centre crop free of sharp paths, dark-map ladder crops clean in both slit variants, burst frames, corner parallax), interact smoke test. |
| 5 | 2026-07-20 | Main-screen light v4 — **«Прорезь» (the slit)**: the Кресты emblem (`sign.svg`) rendered as light through a cross-shaped slit — light pushed toward the viewer through the emblem's strokes. First texture in the ray field: `sign.svg` rasterized to a mask and fed to both renderers via new `setSignMask()` (WebGL2 `TEXTURE_2D`; WebGPU texture+sampler bind group, buffer 16→18 vec4s). New `slitLight()` path (crisp emblem core + bloom halo + radial god-rays, cursor parallax/lean, prism fringing, rays fade near centre so the emblem reads) blended over the field by `slitMix`; never-blank fallback to the cross glyph if the mask fails. New default variant `prorez`/«Прорезь» + kept `prism`/«Призма»; **«Объектив»/«Диско» killed** from the switcher (`?fx=` data only). «Призма» cleanup: **laser "bullet" motes removed** (`moteAmount:0` — this was the cursor-crossing "rave jump"), **link shadow re-anchored** to the label's outer edge (fixes "shadow starts mid-link" + noise contour), calmer shimmer/drift. New params `signSize`/`godrays`/`bloom`, new state `slitMix`. Verified: typecheck, screenshots on WebGPU + WebGL2 (emblem at rest legible, cursor-corner parallax + prism, dusty warm-amber over the dark map on hover, «Призма» cursor-sweep smooth through centre with no jump). |
| 5 | 2026-07-16 | Map round 4: baked icon splay REJECTED ("Egyptian pyramids", user revert `aadd6d2`) → **plan-oblique (military) projection**. Camera pinned straight top-down forever; cursor/touch tilt now drives a pure shear on a wrapper group (`x' = x + sx·y, z' = z + sz·y`), so every roof keeps its exact undistorted 2D plan at all times while walls extrude as parallelograms on the far side (user scheme `references/perspective-guide.png`). Works for any stacked/curved volumes; depth = height keeps z-buffer occlusion exact. All `uRev*`/`patchMaterial` reverse-perspective machinery deleted. Dials: `MAX_SHEAR 0.55`, `?ob=<k>`. Verified: headless rest/up/left/corner on :5299 match the guide. |
| 4 | 2026-07-16 | Feedback round 3: **camera-tilt optics** — cursor acts as a cameraman tilting the lens (`angleWarp`: toward-beams contract into hard bright rods, far side opens into soft fans, per-beam falloff); **fiber bundles** replace periodic prismatic bands (3 generations at different frequencies, per-fiber 4–9 s lifecycles, angular drift, prism dispersion) — kills the "ladder" artifact by construction; **two-mode light** (`u_modeMix`): holographic white/rainbow over blue → dusty amber (dust ×2.7 inside cones, dispersion off) when the scene darkens (hover, or new delayed showreel dark veil); **hover shadow casting** — the hovered label occludes the radial field analytically (penumbra wedge via new `u_linkDist`/`u_linkHalfAng`) and goes near-black in CSS; occasional chromatic **lens ghosts** on the camera axis; **gyroscope wind** on mobile (best-effort deviceorientation); segmented control restored with three new solutions «Призма»/«Объектив»(default)/«Диско», 600 ms preset crossfade, legacy presets via `?fx=`. Artifacts fixed: ladders, "rice seed" mote clipping, fiber subpixel moiré, dust "onion shell" arcs (two root causes in the polar domain — r-scaled parallax warp and r-scaled angular frequency). WGSL buffer 12→16 vec4s. Verified: typecheck, interact smoke test, screenshot suites on WebGPU + WebGL2 (cursor corners, hover shadow, dusty idle, all 3 variants). |
| 3 | 2026-07-15 | Map round 3: TRUE icon reverse perspective. Root-caused the round-1 "fisheye" (one global divergence center shears off-center buildings; icon perspective is polycentric) and replaced it with **per-wall splay baked into the GLB geometry at load** (`reversePerspective.ts`: position-weld → wall-face flood fill per strip → outward push `SPLAY_K·(ownRoofY − y)` along area-weighted horizontal normals; normals kept for flat façade shading). From straight above every building now shows its roof PLUS all four façades (user drawing `references/reverso.png`); splay always on, tilt untouched (camera-side), zero per-frame cost. Superseded `patchMaterial`/`uRev*` global-divergence shader patch removed; `?rp=<k>` dev dial added (`?rp=0` flat plan). Verified: headless screenshots rest/tilt/`?rp=0`/`?rp=1` on :5299. |
| 2 | 2026-07-15 | Feedback round 2: single-variant mode — «Cross Flare» only, segmented control removed (`?fx=` kept as dev override); "tornado" haze killed (secCount 0, dust 0.35, swirl → downwind push) per new reference `light-ref-new.jpg`; cursor-as-wind made unmistakable (beams lean+brighten toward cursor, far side dims, cross arm reaches out ×2.4, motes accelerate/stretch/brighten downwind); hover now blazes the hovered link's whole quadrant via new `u_linkAngles` uniform (pow⁵ zone lobe, tempered 1.35×/0.55L after first pass swallowed labels); WGSL buffer → 12 vec4s. Verified: center vs cursor-right vs hover screenshots. |
| 1 | 2026-07-15 | Feedback round 1. Rays: beams moved to bisectors BETWEEN links (upright cross), living motion (global sway ±8°/45s + per-beam wander), outer glow halo, prismatic refraction bands (RGB-phased, `refraction` param), per-beam shimmer, wow pass on presets (v2/v4 heroes, v2 = new default); hover reworked into "gallery dark" scene change — near-black drop + per-link photo under screen-blended canvas + `u_sceneDim` light surge/warm. Map: REPLACED per-building scaling with user's real GLB (`scene.glb`, normalized span 300) + OrthographicCamera exactly top-down by default (`up=(0,0,−1)`), cursor-deadzone tilt (0.08, max 0.5 rad toward cursor), and TRUE vertex-level reverse perspective via `onBeforeCompile` view-space divergence (`K=0.85`, `refScale=110`) — walls splay icon-style, reads clearly. Verified via playwright screenshots (v2/v4 idle+hover, map top-down + tilt, both backends live on :5199). |
| 0 | 2026-07-15 | Bootstrap + full prototype in one session: Vite + vanilla TS scaffold; main screen (scale-to-fit 1440×800 stage from Figma node 58:410, inline logo, 4 rotated ALS Chromius nav links, 10s news ticker, «КРЕСТЫ · 2026» mark); ray-field uber-shader in WebGL2 GLSL + WebGPU WGSL twins (4 fixed primary beams at runtime-measured link angles, rotating secondaries, dust fbm, Cartesian-cell radial motes, cross glyph, chromatic fringe, filmic shoulder, grain+dither); 5 effect variants + segmented switcher (`?fx=`, `[`/`]`); per-link hover modes; idle photo showreel; Концепция screen (Three.js extruded footprints, matcap+edges, cursor/touch tilt, reverse perspective `(d/ref)^1.5`); converge/flash/release transition (720ms) + hash router with popstate; perf tiers; artifacts fixed: polar sonar rings, star-trail motes, cell-clipped rectangles. Verified via headless Chrome (both backends, all 5 variants) + playwright-core interaction script (`scripts/interact-test.mjs`). Hygiene: CLAUDE.md, .claude/ (settings, rules, commands), docs/, memory/, git init. |
